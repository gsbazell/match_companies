# Additional dependencies for the MCP server (mcp_server.py)
# Install alongside the core: pip install -r requirements.txt -r requirements-mcp.txt
-r requirements.txt
mcp>=1.0.0
openpyxl>=3.1.0   # Excel file reading for prepare_csv tool
