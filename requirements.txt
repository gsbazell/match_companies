# Core dependencies for company matching tool
openai>=1.0.0
numpy>=1.20.0
Unidecode>=1.3.0  # Optional but recommended for accent handling

# MCP server (agentic skill interface)
mcp>=1.0.0
openpyxl>=3.1.0   # Excel file reading for prepare_csv tool
